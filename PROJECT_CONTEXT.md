# MISO Incentive 프로젝트 컨텍스트

> 앱의 구조, 확정 정책, 권한처럼 자주 바뀌지 않는 기준을 기록한다. 최근 작업과 장애는 `HANDOFF.md`, 날짜별 변경은 `CHANGELOG.md`에 기록한다.

## 프로젝트

- 서비스명: 미소페이
- 저장소: `leeblack4878-cyber/miso-incentive`
- 프런트엔드: React + Vite 기반 PWA
- 데이터·인증: Supabase
- 운영 배포: GitHub `main` 반영 후 Vercel 자동 배포
- 주요 코드: `src/App.jsx`, 공용 계산은 `src/appShared.jsx`, 실적입력·고객관리·홈 설치관리와 관리자 기능은 `src/components/`로 단계적으로 분리. 실적입력·고객관리·평가·급여·관리자 화면은 진입 시 지연 로딩한다. 월별 실적·목표 표시는 MonthlyPerformance.jsx, 공유 기본값/매장 조회는 viewShared.js에서 관리한다.

## 데이터 기준

- `daily_records`: 날짜별 구버전/일일 집계와 입력 상태
- `monthly_status`: 월 상태 및 월 단위 draft
- `customer_sales`: 고객별 판매 내역
- 모바일·고객별 기타 판매는 `save_sale_atomic`, 홈 묶음은 `save_home_bundle_atomic`으로 저장한다. 직원 정보 승인과 커리어 최종 결정도 전용 RPC로 관련 자료를 함께 반영한다.
- 판매 수정은 완료·취소 약속 이력을 보존한다. 모바일 편집은 수정 전 판매·약속·비용·일일 집계를 대조하고 오래된 화면의 덮어쓰기를 거부한다.
- 새 모바일 스팟은 `source_sale_id`로 판매와 연결해 삭제 시 함께 취소한다. 기존/홈 스팟의 연결 대상은 추정하지 않는다.
- `home_orders`: 홈 청약·설치 상태의 원본
- 홈 실적은 청약일이 아니라 **설치 완료일이 속한 월**에 인정한다.
- 신규 홈 데이터가 있으면 완료된 `home_orders`의 고객·일자 묶음 건수를 우선 사용한다.
- 구버전 데이터만 있으면 기존 `daily_records` 집계를 호환용으로 사용한다.
- 취소 상태는 내역, KPI, 목표, 순위, 평가, 예상급여에서 제외한다.

## 핵심 운영 정책

- SIM MNP는 HS 건수에 포함하지 않는다.
- 홈 구조·품질 정책은 가정망만 인정하며 소호는 제외한다.
- 홈 실적 및 홈 수수료는 설치 완료 기준이다.
- 담당·대표·허용된 운영자가 특정 매장을 선택해 입력한 지원 판매는 개인 실적에서 제외하고 선택 매장의 팀 실적에만 반영한다.
- 무료폰/인센미지급 특가는 요금제·VAS·보험 인센티브를 제외하되 영업활동 지원비 건수는 인정한다.
- 2ND 단독·번들은 모두 생산성 및 성과에서 0.2P로 인정한다.
- 폰안심패스는 인센티브 0원, 보험 전략P 0.8P다.
- 월별 정책은 해당 판매월의 정책 버전과 저장된 정책 스냅샷을 우선한다.
- 사용자가 매월 말일 또는 1일에 새 정책을 제공한다. 2026년 10월부터 해당 월 정책 반영 전에는 입력 캘린더에 “○월 정책 입력 전”을 표시하고 실적 입력을 잠근다.
- 새달은 기본 잠금이며 기존 관리자 입력 열기 기능으로 해당 월만 준비 완료 처리한다. 이전 달의 입력 허용을 다음 달에 자동 승계하지 않는다. 정책 설정 조회 전·실패도 입력 잠금을 유지한다.

## 권한 구조

- 일반 직원: 본인 실적·급여·고객 업무
- 점장·운영 권한 부점장: 본인 매장 운영 범위
- 영업담당: 지정 상권 범위
- 대표 및 이강진: 전체/상권/매장 조회 범위
- 회사 전체 수익 및 민감 관리 기능: 허용된 운영진·대표 범위
- 직원 시스템 권한 변경은 지정된 권한 관리자만 수행하며 변경 이력을 남긴다.

세부 인물·매장 매핑은 DB 정책 및 적용된 Supabase 마이그레이션을 기준으로 대조한다. 이름만 보고 권한을 새로 추정하지 않는다.

- 지정 인물의 권한 판정은 `src/permissionScopes.js` 및 DB의 불변 계정 ID 기준이다. 표시 이름 변경이나 동명이인 가입은 지정 권한을 이전하지 않는다.
- ID를 새 계정으로 이전해야 할 때는 기존 관리 범위 및 계정 소유자를 확인해 별도 변경한다. 이름 자동 매칭으로 대체하지 않는다.
- 지급기준·권한 관리 UI는 `src/components/`에서 지연 로딩하며 공유 표시 상수/서식은 `src/uiDefinitions.js`를 사용한다.

## 절대 임의 변경 금지

- 지급 단가, 목표값, 계산 구간 및 적용월
- 직급·역할·권한 범위와 인물별 관리 범위
- 위탁/자가 매장 구분
- 마감·승인·평가 기준
- 취소건 제외 및 설치 완료월 인정 원칙
- 지원 판매의 개인 제외·팀 반영 원칙
- 운영 데이터 삭제·일괄 보정

위 항목은 사용자의 명시적 요청 없이 변경하지 않는다. 버그 수정 과정에서 계산 결과가 달라질 때도 변경 전후 숫자를 먼저 설명한다.

## 필수 회귀 점검

- 모바일 판매: 등록 → 수정 → 실행취소 → 삭제
- 홈 판매: 작성 중 → 설치완료 → 취소 → 수정 → 삭제
- 구버전 홈 복원 및 취소·삭제
- 개인/관리자/내역/KPI/목표/순위/예상급여 간 숫자 일치
- 지원 판매가 개인 실적에 포함되지 않고 팀 실적에만 반영되는지
- 월 변경 시 과거 정책 보존
- 권한별 조회·수정 범위
- 모바일 긴 문구와 레이아웃 넘침

## 화면 디자인·금액 표시 기준

- 공통 palette는 tailwind.config.js의 brand(파랑), ink(차콜), gray(중성 회색). 사용자 확정 디자인은 토스처럼 밝은 바탕·큰 핵심 숫자·넉넉한 여백. 경고/실패/완료 색상은 상태 의미를 유지한다.
- 모바일에서 긴 제목과 실제 금액을 잘라 숨기지 않는다. 표가 좁으면 항목별 배치로 전환한다.
- 직원 홈 예상 마감과 내역 최종 합계는 payDisplay.js를 사용한다. computePay의 결과에 승인 일반/홈 스팟을 더하고 비용을 빼며, 이미 포함된 모바일 스팟을 중복 가산하지 않는다.
- 원래 실적 수수료/정책 엔진 결과와 비용 반영 후 금액은 구분한다. 조회 실패를 0원으로 간주해 확정 합계를 표시하지 않는다.

## 2026-09-18 확정 한시 정책

- 홈 9/18~21 청약·9월 설치 완료, 프리미엄 안심 보상 500MB↑ + 방송패스(소호 프리미엄↑). 소호 포함 3건 달성 시 가정망 전체 건당 15만원, 1~2건 10만원. MNP 동시판매 10만원 추가. 이전 주말과 등급 별도. 사용자 확인상 인터넷은 전부 프리미엄 안심으로 가입하므로 별도 요금제 선택이나 메타 조건은 두지 않는다.
- 아이폰18 사전예약 9/18~21 개통: MNP 할인30만원/기변15만원, 115군↑(청소년85 인정)·전략2P, 직원 요금제/VAS/보험 미지급.
- S26 울트라 MNP·기변 추가지급은 모두 8만원이다. 모델 추가지급 변경은 9/18 신규 정책 키로 관리한다. 유통망 지원금 입력/할인액/관련 실적 제외는 적용 범위에서 제외한다. 수치와 기간의 상세 원본은 `src/septemberPolicy.js`에 있다.

## 고객 약속·중고폰 관리

- 모바일 실적의 안내 선택은 source_meta.reminders에 저장하고 개통일(판매일) +93일/+183일의 고객 확인 약속을 생성한다. 실제 변경·해지를 자동 실행하지 않는다. 유지 선택은 약속을 생성하지 않는다.
- 고객별 수동 약속은 source_sale_id 없이 customers에 귀속하며 신규 약속의 선택 판매 건은 task_meta.sale_reference에 스냅샷으로 연결한다. 판매 교체·삭제 시 독립 약속을 보존하고 이름만으로 다른 판매에 재연결하지 않는다. 고객 목록의 월은 sale_date(실적 입력일) 기준이며 모바일·홈 판매를 각각 선택한다. 중고폰 금액과 처리 방식별 배분은 customer_tasks.task_meta에 저장하며 급여 비용으로 자동 차감하지 않는다.
- 월 중고폰 처리금액은 status=completed이고 processed_date가 선택월인 실제금액을 합산한다. 처리 방식별 배분 합계는 실제금액과 일치해야 완료할 수 있다.
