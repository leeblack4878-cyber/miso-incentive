# MISO Incentive 작업 원칙

이 저장소에서 작업하는 모든 에이전트는 아래 순서를 지킨다.

## 작업 시작

1. `PROJECT_CONTEXT.md`를 읽어 앱 구조, 운영 정책, 권한, 금지사항을 파악한다.
2. `HANDOFF.md`를 읽어 최근 작업, 현재 장애, 미완료 항목을 파악한다.
3. `CHANGELOG.md`와 최근 Git 커밋을 확인해 실제 반영 상태를 대조한다.
4. 수정 전에 현재 상태와 이번 작업 범위를 10줄 이내로 사용자에게 알린다.
5. 문서와 코드가 다르면 추측하지 말고 코드·DB 마이그레이션·테스트를 근거로 확인한다.

## 작업 중

- 기존 지급정책, 목표값, 직급, 권한 범위를 임의로 변경하지 않는다.
- 특히 `PROJECT_CONTEXT.md`의 **절대 임의 변경 금지** 항목은 사용자 확인 없이 바꾸지 않는다.
- 정책 계산 변경은 실제 숫자 사례를 포함한 테스트를 추가한다.
- 등록·수정·취소·삭제는 개인 화면, 관리자 화면, 내역, KPI, 예상급여를 함께 점검한다.
- 구버전 집계와 신규 원본 데이터가 함께 존재할 수 있음을 전제로 한다.
- 모바일에서 긴 문구, 줄 넘침, 버튼 겹침을 확인한다.
- unrelated 변경이나 사용자의 기존 변경을 덮어쓰지 않는다.

## 작업 종료

1. 관련 테스트와 배포 빌드를 실행한다.
2. `HANDOFF.md`의 현재 상태, 최근 완료, 미해결 문제, 다음 할 일을 갱신한다.
3. 사용자에게 보이는 변경이면 `CHANGELOG.md`에 날짜별로 기록한다.
4. 장기 정책·권한·구조가 확정 변경됐을 때만 `PROJECT_CONTEXT.md`를 갱신한다.
5. 문서 변경도 코드와 같은 커밋에 포함한다.
6. 사용자 승인 범위에 따라 푸시·배포하고, 배포 성공 여부를 확인한다.

## 문서 우선순위

충돌 시 실제 코드와 적용된 DB 마이그레이션을 우선 확인한다. 문서는 즉시 현재 상태에 맞게 고치되, 정책이나 권한의 의도가 불명확하면 사용자에게 확인한다.
