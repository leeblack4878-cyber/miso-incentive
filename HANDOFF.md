# MISO Incentive 인수인계

## 2026-09-20 (비활성 실적 보존·실적 초기화 승인)

- profiles는 승인된 비활성 직원도 읽고 active를 보존한다. scopedPerformanceEmployees는 기존 권한 범위의 이력 집계, scopedEmployees는 현재 활성 인원·입력 선택·미입력 알림에 사용한다. 비활성은 해당 월 일일/월별/홈/판매/스팟 이력이 있을 때만 rows에 포함해 빈 미래 월에 기본 급여를 생성하지 않는다. 지급 계산식은 변경하지 않았다.
- 일일·홈·판매 원장과 일반 직원 매장 익명 집계를 페이지별 조회한다. get_my_store_performance_days는 활성 조회자 확인·고객정보 제거를 유지하고 구성원의 active 조건만 제거했다. 브리핑 완료 홈과 성과 달력 합계에도 이력을 포함하되 미입력·휴무 인원은 활성만 집계한다.
- performance_reset_requests: SELECT RLS(본인/기존 can_write_target 범위), 직접 쓰기 권한 없음. private guarded RPC + public invoker wrapper. 요청자≠승인자, 승인 만료 24시간, 대상월 잠금, 활성 승인 계정, 요청 행 잠금·1회 실행. 승인 자체로 데이터를 삭제하지 않는다. 기존 reset RPC도 승인 검사 후 내부 전용 함수로 실행하므로 구버전 화면으로 우회할 수 없다.
- 기존 초기화 본문을 private로 이동해 직접 호출 권한을 회수했다. 기존 삭제 범위/백업 방식은 유지하며 실행 완료 요청에 backup_id를 연결한다. 수동 약속(source_sale_id=null)은 유지한다.
- 단위/계약 관련 91개 및 빌드 통과. staging 트랜잭션 테스트에서 비활성 포함 5+3=8, 익명 정보, 권한 밖·본인 승인·미승인·중복·반려·재실행·비활성·마감월 차단, 승인 시 데이터 보존과 실행 직전 백업을 검증 후 전체 롤백했다. 320px 직원/관리자 UI 회귀 추가; 최종 CI·배포 상태는 PR 확인. 운영 데이터 초기화는 실행하지 않는다.

## 2026-09-19 (월별 고객 목록·판매 건별 약속)

- 고객별 약속관리: 기본 선택월, 월별 optgroup(전체 기간 포함), 고객명 검색. 고객 ID 대신 판매 ID별로 선택하며 모바일/홈/기타, 입력일(실적에 지정한 sale_date), 상품명과 짧은 건 번호 표시. 생성시각·설치 완료일로 월을 바꾸지 않는다.
- 홈 취소 여부는 home_orders와 source_ref를 대조한다. 빈 삭제 고객은 제외하고 미완료 약속 및 완료·취소 탭의 이력은 보존한다.
- 신규 수동 약속은 task_meta.sale_reference에 선택 판매 ID·날짜·종류·상품명 스냅샷을 보관한다. source_sale_id는 기존대로 null로 유지해 홈 교체 저장/판매 삭제 시 독립 수동 약속이 소실되지 않는다. 삭제/교체된 판매의 약속은 이전 판매 연결로 남긴다. 같은 이름만으로 새 판매에 재연결하지 않는다.
- 기존 연결 없는 약속은 기존 약속 항목으로 분리하며 날짜가 없으면 날짜 미확인에서 조회한다. 기존 운영 데이터 일괄 보정·스키마·지급 계산 변경 없음.
- 단위·계약 228개와 빌드 통과. 월별 동명이인·홈·취소·삭제·구버전 분리, 320px 월 전환/검색/선택 건만 표시/분할 금액 저장 회귀를 추가했다. 최종 CI·배포는 PR에서 확인한다.

## 2026-09-19 (고객 약속 우선 표시·고객 선택 목록 정리)

- 고객 선택 목록은 남은 판매 또는 진행 중 약속이 있는 고객만 표시한다. 완료·취소 탭에서는 이력만 있는 고객도 조회하며 원본 고객·약속은 삭제하지 않는다.
- 오늘/경과/7일 내 요약과 약속 목록을 먼저 표시하고 중고폰 월별 합계는 하단 접기 영역으로 이동한다.
- 삭제 후 빈 고객 숨김, 수동 약속 유지, 이력 접근과 320px UI 회귀를 추가했다. 검사·배포 결과는 PR에서 확인한다.

## 2026-09-19 (요금제 선택 후 안내 팝업)

- 요금제군을 선택하면 즉시 93일/183일/둘 다/유지 팝업을 표시한다. 선택 시 닫히며 요금제 아래 요약 버튼으로 다시 변경한다. 기존 저장 선택은 유지하고 팝업 닫기로 임의 변경하지 않는다. 최근 조합 제거와 함께 반영한다.

## 2026-09-19 (최근 판매 조합 제거)

- 사용자 요청으로 실적 입력의 최근 판매 조합 빠른 선택과 관련 상태·브라우저 저장/복원 로직을 제거했다. 판매 입력·저장·안내 약속 로직은 유지한다. 빌드와 기존 모바일 입력 화면 검사로 확인한다.

## 고객 약속 입력 간소화·중고폰 관리 (2026-09-19)

- 실적 입력에서 요금제 93/183일/둘 다/유지, 선택한 부가서비스별 93일 삭제 안내/유지, 보험(폰교체패스·폰안심패스) 93일 확인/유지를 선택한다. 기본값은 유지이며 선택한 안내만 생성한다. 개통일은 기존 판매일 기준이다.
- source_meta.reminders에 선택을 저장하고 기존 save_sale_atomic에 약속을 함께 전달한다. 완료·취소 이력은 RPC에서 보존하며 다시 만들지 않는다. 구버전 통합 부가서비스 약속·진행중 수동 약속과 변경한 안내 예정일·메모도 보존한다.
- 고객관리의 고객 목록은 기존 customers를 사용한다. 중고폰/오퍼/제휴카드/케이스/기타 약속을 고객별로 추가·수정한다. 수동 약속은 고객에 직접 귀속하며 판매 수정·삭제와 별개로 유지한다. 기존 제휴카드 단계 기능 유지.
- 중고폰 예상·실제금액, 처리 방식별 배분, 기타 메모, 처리 완료일은 task_meta에 저장한다. 완료 시 배분 합계와 실제금액 일치를 검사한다. 월별 합계는 해당 완료월·완료 상태의 실제금액을 한 번만 집계한다. 0원과 미입력 구분. 완료 취소 시 합계 제외. 기존 사용자 조회 범위 유지.
- 고객·약속 조회는 1000행 페이지 처리, 조회 실패 시 합계 대신 오류 표시. 수정은 기존 updated_at 비교와 단일행 응답 검사 유지. DB 스키마/지급정책 변경 없음.
- 단위·계약 224개 및 빌드 확인. staging authenticated 역할로 고객·중고폰 약속 생성/완료/JSON 재조회 확인 후 전부 롤백. 320px 입력·배분·재조회·완료취소 UI와 기존 실제 로그인 모바일 등록/수정/삭제의 둘 다 일정 검증을 CI에 추가. 최종 CI·배포 결과는 PR에서 확인.


## 브리핑 집계 정정 (2026-09-19)

- SIM MNP는 중고 MNP 결합 그룹 대신 모바일 matrix[5]를 집계한다. 2ND는 단독 matrix[7]와 번들을 합산한다. 같은 관리자 지표 함수에도 적용한다.
- 브리핑은 선택일까지의 일일 원본으로 누적 건수·생산성을 재계산한다. 홈 원본은 당월 설치 완료일과 선택일로 제한하며 전월 청약의 당월 설치를 포함한다. 원본·정산 지급 계산은 변경하지 않는다.
- 운영 대조: 산본 9/18 SIM MNP 실제 5건, 목표 12건 → 예상 8.3건·69%. 스홈 완료 1건, 목표 6건 → 예상 1.7건·28%.
- 관련 단위 10개와 빌드 통과. 선택일 변경·SIM/2ND 실제 화면 검사를 추가했다. 배포 증거는 이번 PR에서 추적한다.

## 9월 18일 사용자 정정 (2026-09-18)

- S26 울트라 MNP도 기변과 동일한 추가 8만원으로 정정. 9/18부터 적용하고 이전 5만원은 보존한다. 운영 조회에서 정정 전 키로 저장된 9/18 이후 모바일 판매는 0건이었다.
- 사용자가 홈은 모두 프리미엄 안심 가입이라고 확인했으므로 인터넷 요금제 선택/필수 검사/메타에 따른 지급 제외를 제거했다. 신규 입력은 해당 기간 프리미엄 안심으로 기록하고 기존 메타가 없는 건도 동일하게 계산한다. 요금제 조회만을 위한 중복 API 요청도 제거했다.
- 기존 9/18 완료 1G+방송패스+MNP 묶음은 수정 저장 없이 주말 추가 20만원(기본10+MNP10)이 반영된다. 속도·TV·망·청약/설치기간 등 다른 조건은 유지한다.
- 수치·회귀 216개 및 빌드, 인터넷 요금제 선택 제거/울트라 8만원 화면 검사를 수행한다. 최종 배포 증거는 정정 PR에 기록한다.

## 9월 18일 추가 정책 (2026-09-18)

- 자료: 사용자 제공 회사 공지 3장. 홈 청약 9/18~21·9월 설치, 아이폰18 사전예약 개통 9/18~21, 모델 추가지급 9/18부터 별도 공지까지.
- 홈: 프리미엄 안심 보상 500MB↑ + 가정망 방송패스(첫 달 유지), 소호 프리미엄↑ TV. 소호도 3건 등급에 포함하되 가정망만 지급. 사용자 확인에 따라 3건 달성 시 전체 건당 15만원(1~2건은 10만원). 가정망 MNP 동시판매 +10만원. 1G 추가 5만원은 이전 9/11~14에만 유지한다. 두 주말 건수를 합산하지 않는다.
- 인터넷 요금제를 `customer_sales.source_meta.internetPlan`으로 저장·수정 복원하고 개인/관리자/정산 조회에서 home_orders에 결합한다. 최초 반영 시 요금제 선택을 받았으며 위 사용자 정정으로 별도 선택과 메타 조건을 제거했다. 스키마 변경 없음.
- 아이폰18 MNP 고객 할인 30만원/기변 15만원을 인센미지급 특가에서 선택한다. 115군↑ 또는 청소년85·전략2P·가입구분·기간 확인. 요금제/VAS/보험 미지급, 실적·활동건수·별도 2ND 처리는 기존 미지급 정책 유지. 고객 할인은 직원 지급·차감액으로 넣지 않는다.
- 판매 활성화: S26 10만원, S26+ 8만원, 울트라 MNP·기변 8만원, F971-256 10만원, F976 5만원. 다른 추가지급은 공지대로 유지하며 F971-512 기존 5만원 유지. 과거 행과 새 정책 키를 분리하여 9/17 이전 금액을 보존한다. 유통망 입력/지원금/실적 제외 지시는 사용자 요청대로 추가하지 않는다.
- 홈 추가지급을 고객별 내역으로 표시한다. 관리자 정산 상세에서도 추가지급은 기존 수수료를 유지하고 인센미지급만 차감한다. 가입유형/요금제 변경 시 정책을 다시 조회하거나 선택을 초기화하지 않는다.
- 단위·수치·저장복원 회귀 214개 및 빌드 통과. 정책 선택·320px 홈 입력 UI와 기존 실제 로그인 홈 저장 회귀는 이번 PR의 CI에서 확인한다. 공용 계산 변경으로 서버 배지 번들도 재생성했으며 배지에 사용하는 건수·생산성 기준은 변경하지 않는다.

## 평가 입력기준 분리 (2026-09-18)

- 직원입력기준 / 회사입력기준을 분리하고 선택값을 매장·월별로 저장한다. 회사 0건과 미입력을 구분하며 회사값 미등록 시 직원값을 자동 혼합하지 않는다.
- 회사 실적 기준일·저장일과 직원/회사 차이를 표시한다. 회사 입력 누락 시 점수·관리자 예상액을 보류한다. 현재 월의 미설정 기본값은 직원입력기준이다.
- 조회 오류/매장 전환의 오래된 응답을 차단한다. 저장은 기존 JSON/확인시각 비교와 단일행 응답 확인으로 동시 수정 및 권한 오류를 감지한다. 기존 정책·접근권한·DB 구조는 유지한다.
- 제공 평가표는 입력과 기준일이 일치할 때만 사용하며, JSONB 객체 키 순서 변경은 동일 자료로 인정한다. 회사 실적 수치 자료의 일괄 반영은 이 코드 업로드와 별도로 진행한다.
- 단위·SDK 208개와 배포 빌드 확인. 새 UI 검증은 기준 전환/저장 실패/미입력/320px 입력 화면을 포함하며 최종 서버 검사·배포 결과는 이번 PR에서 확인한다.

## 출시 점검 후 취소·배지 검증 보완 (2026-09-17)

- 홈 취소 시 연결된 진행 중 약속을 함께 취소한다. 비용은 실제 지출 유지/정산 제외를 명시적으로 선택한다. 제외는 원래 금액과 이력을 보존하며 개인·관리자 합계에서 빠진다. 완료 약속·제외 비용은 홈 재편집에도 보존한다. 완료 건은 먼저 진행중으로 되돌린 후 취소한다.
- 취소는 기존 RLS 아래 단일 트랜잭션이며, 직접 상태 변경·구버전 취소 호출도 비용 선택 없이 통과하지 않는다. 취소 복원은 약속/비용을 자동 복원하지 않는다. 기존 운영 취소건을 일괄 수정하지 않는다.
- 자동 배지는 서버의 저장 자료와 공용 정책 함수로 계산한다. 다른 직원의 전체 자료는 서버 내부에서 순위 계산에만 사용한다. 지원판매는 개인 배지에서 제외. 브라우저의 임의 자동 지급 및 지급 RPC 직접 호출을 차단한다.
- 과거 자동 배지는 삭제하지 않고 검증 대기로 구분한다. 해당 실적 월의 서버 검증을 통과한 배지와 기존 수동 지급 배지만 획득/대표배지로 사용한다. 기존 근속·판매 조건은 유지한다.
- SQL 원본: sql/launch_home_cancellation.sql, sql/preserve_home_closed_history.sql, sql/launch_badge_validation.sql. 서버 함수: supabase/functions/sync-badges. npm run build:badges로 같은 앱 계산식을 번들한다. 서버 키는 클라이언트에 포함하지 않는다.
- staging 적용 및 RLS SQL: 취소 중 비용 처리 실패 전체 롤백, 부분 취소, 비용 유지/제외, 완료 이력 보존, 타인 차단, 배지 위조 차단 통과. 실제 staging Auth/브라우저/DB 9개 통과(run 35224564279): 홈 수정·반복 저장·전체 롤백·타인 접근, 취소 비용 제외/유지 및 배지 위조·타인 실적 분리·중복 지급 차단. 단위 203개, 전체 UI 34개 + 320px 취소 UI 2개 통과. 최종 CI·운영 배포 증거는 PR #24 본문에서 추적한다.
- 10월 확정 정책과 직원·점장의 최종 업무 리허설은 별도 도입 조건으로 남아 있다.


## 홈 완료 지원판매 JSON 조회 오류 (2026-09-17)

- 단건/묶음 완료의 source_refs JSONB 포함 조회에 JS 배열을 전달하여 PostgreSQL 배열 문법으로 직렬화되던 오류 수정. JSON.stringify로 JSON 배열을 전달한다.
- 조회 실패 시 완료 중단, 기존 RPC·정책·권한·완료월·지원판매 귀속은 유지. 운영 고객 데이터 직접 변경 없음.
- 실제 SDK 요청 직렬화 회귀 추가. 단위·계약 199개 및 빌드 통과. 브라우저/서버 검사와 운영 배포 결과는 이번 수정 PR에서 확인한다.

## 개인·매장 실적 카드와 진척도 상세 (2026-09-17)

- 기준 main d66bbc7(PR #21). 사용자 승인: 현재 밝은 파랑 UI 유지, 개인/매장 2열 현재 실적 카드, 높이를 조금 줄이고 상세보기로 목표·달성률·예상 마감·전월 대비 이동 후 즉시 배포.
- PerformanceCard 공통 표시를 개인·직원 매장·관리자 핵심 성과에 적용. 숫자 18px 유지, 카드 상하 여백 12→10px. 개인 목표 편집과 날짜별 내역은 상세보기에서 접근. 기존 목표 저장 경로·지급정책·권한·DB 구조 변경 없음.
- 상세를 열 때만 비교 자료/매장 목표 조회. 비교는 한국 날짜의 당월 경과일과 전월 동일일까지, 과거 월은 각각 전체 월. 현재 조회 대상의 일일 입력/설치 완료를 양쪽 모두 재집계하며 월 단위 legacy draft·본사 확정 실적은 날짜 배분이 불가능해 비교에서는 제외. 이전 재직자·매장 이동 이력 재구성은 하지 않음.
- 이전 달 정책 이력을 적용하고, 기존 취소 홈 잔여 차감·설치 완료 기준·지원 판매 팀 귀속을 유지. 일반 직원의 매장 비교는 기존 비식별 RPC만 사용. 일일 자료는 복합 정렬과 1000행 페이지 조회. 자료 없음/조회 실패/전월 0건을 구분.
- 관리자 매장 선택 연동 유지. 직원 매장 상세의 예상 마감도 본사 확정값을 재확대하지 않도록 관리자와 동일한 한국 날짜 기준 사용.
- 단위·계약 198개 통과. UI 전체 최초 33/34 통과 후 테스트의 완료 홈 상품을 실제 인터넷 상품 형식으로 수정하여 관련 8개 재검증 통과. 320/390px 카드·상세 스크린샷 확인, 768px 기존 회귀 포함. 최종 CI·main·Vercel Production 증거는 이번 PR 본문에 기록.

## 관리자 매장 범위·실적 예상 마감 (2026-09-17)

- 기준 PR #20 main 4640c45. 밝은 파랑 UI 유지, 사용자가 수정·즉시 배포 승인.
- 성과 달력 내부에만 있던 매장 선택을 관리자 홈 공통 상태로 이동. 핵심 성과·인원·직원 목록·고객 요약·선택 매장 목표에 같은 범위 적용. 조회 가능한 직원/매장 범위는 기존 권한 필터 그대로 사용. 전체 매장 챌린지 순위는 비교용 기존 범위 유지.
- 핵심 성과에 실적 예상 마감 추가. 현재 월은 한국 날짜 경과일 비례 환산, 다른 월 및 본사 마감 확정 매장은 확대하지 않음. 지급액 계산과 별개 표시.
- 직원 홈의 본사 비교 카드는 내역으로 이동. HS/2ND/성과P 차이가 표시 정밀도 이상일 때만 홈에 이동 안내. 본사 자료 없음과 조회 실패를 구분.
- 첫 실제 Auth CI에서 온보딩 안내창 뒤 배지를 먼저 누르려는 테스트 경합 검출. 안내창 닫은 후 배지 핸들러를 등록하도록 순서 수정. 강제 클릭이나 테스트 생략은 하지 않음.
- 단위 및 브라우저 회귀는 선택 전환, 점장 범위, 담당 상권, 확정값 재환산 방지, 본사 미등록/차이/오류를 포함. 최종 검사와 배포 증거는 이번 PR 본문에 기록.

## 밝은 파랑 UI 적용 (2026-09-17 KST)

- PR #19 main e0429b5 운영 배포 후 사용자가 네이비·청록을 거절하고 토스형 시안을 선택했다.
- 흰색 급여 카드, 차콜 숫자, 파랑 주요 버튼, 중성 회색 배경으로 공통 palette 변경. 오늘 할 일은 타일 대신 아이콘/항목/건수 목록. 급여 비교·배지·프로필·기존 메뉴와 상태는 유지.
- 현재 실적 금액을 예상 급여로 바꿔 표기하지 않으며 계산·DB·권한 변경 없음. 입력 버튼은 카드 하단 전체 너비로 배치.
- 앱 아이콘은 기존 트로피 형태에 파랑 배경, PWA 상단은 흰색. 서비스워커 캐시 v5.
- 로컬 단위·계약 193개, 모킹 UI 20개 통과. 급여 카드 상단 이동 후 관련 화면 7개 재확인 및 최종 빌드 통과. 320/390/768px 검사와 390px 실제 스크린샷 확인.
- GitHub 업로드 목적지 확인에 대한 자동 검토 차단 후, 사용자가 기존 leeblack4878-cyber/miso-incentive 업로드·운영 배포를 명시 승인하여 재개. 최종 서버 검사와 운영 배포 증거는 이번 디자인 PR 본문에서 확인한다.

## 디자인·잔여 개선 (2026-09-17 KST)

- 기준 main 52d325e(PR #18). 이전 저장 원자화·권한·정책 잠금 작업은 재구현하지 않았다.
- 사용자 승인: 남은 기능 점검과 UI 개선을 함께 진행하고 검증 후 배포. 보라색 고정 없이 색상 변경 허용. 지급정책·권한 변경 금지.
- 공통 색상은 밝은 슬레이트 바탕, 네이비 요약 카드, 청록 버튼으로 통일. 로그인·직원·관리자·입력·평가·설정에 적용. 앱 아이콘/테마와 서비스워커 캐시도 갱신했다.
- 홈의 중복 미입력 안내, 반복 제목·설명, 목표 미설정의 행별 경고를 줄였다. 정책 잠금·승인 전 금액·삭제 확인 등 판단에 필요한 안내는 유지.
- 휴대폰 목표 현황은 각 항목 아래 목표/실적/진척도/예상 마감을 배치해 잘림을 방지. 관리자 핵심 수치는 작은 화면에서 2열, 메뉴는 줄바꿈. 하단 탐색 선택 표시와 안전영역 여백 보완.
- 평가·관리자 급여를 EvaluationViews.jsx로 옮겨 지연 로딩, 월별 목표·실적은 MonthlyPerformance.jsx로 분리. 계산 본문과 정책 수치는 유지.
- 확인된 표시 불일치 수정: 홈 예상 마감에 누락되던 승인 일반/홈 스팟을 내역과 동일하게 포함. 기본 100만원 + 해당 스팟 3만원 - 비용 1만원 = 두 화면 102만원. 모바일 스팟은 computePay에 이미 포함되어 추가하지 않음.
- payDisplay 공용 표시 함수 사용. 비용·스팟 조회 실패 시 0으로 계산하지 않고 합계 표시 보류. 월/직원 전환은 직원 화면 상태를 새로 만들고 늦은 응답을 무시. 탭/일일자료 변경 시 비용·스팟 재조회.
- 기타 점검: 지원 홈 완료 전 지원판매 조회 실패 차단, 영업비용 중복 클릭 차단/날짜 확인/실패 시 입력 보존, 알림 읽음은 서버에서 바뀐 ID만 표시, 프로필 상태 저장 전 조회 오류 및 대표배지·푸시해제·이전 화면 자동저장 실패 안내 보완.
- 로컬 단위·계약 193개, 모킹 UI 20개, 빌드 통과. 실제 화면을 320/390/768px에서 렌더링하고 스크린샷 확인. 메인 JS 795.74 → 743.57kB, gzip 273.17(PR #17) → 217.89kB. 실제 로그인 회귀/최종 main SHA/Vercel Production 결과는 이번 PR 본문에서 추적한다.
- DB 구조·RLS·역할·단가 변경 없음. 모든 저장 경로의 완전 검증이나 전체 App 분해 완료를 의미하지 않는다. 실운영 계정으로 테스트하지 않는다.

## 최신 개선 및 검증 (2026-09-17 KST, PR #18)

- 시작 기준 main 42fcf50(PR #17). PR #14~17의 삭제 원자화·권한 ID·정책 잠금·홈 하위자료 조회·테스트 안정성 작업은 이미 완료된 것으로 확인했고 중복 구현하지 않았다.
- 모바일 등록/수정 및 고객별 기타 판매를 save_sale_atomic으로 묶어 저장. 고객·판매·약속·비용·신규 스팟·지원판매·일일 합계가 함께 성공하거나 롤백한다. 실행취소는 기존 delete_sale_atomic을 사용한다.
- 편집 전 판매·약속·비용·일일 집계 비교로 오래된 화면의 덮어쓰기를 거부한다. 하위자료 조회 실패 시 빈 편집창을 열지 않는다. legacy daily 판매의 원본 유형과 정책 스냅샷을 유지한다.
- 홈 수정은 완료·취소 약속 ID/생성시각/완료시각/내용을 보존해 새 판매에 연결한다. 고객관리 완료·취소 화면 표시까지 검증.
- 직원 정보 승인과 커리어 최종 결정은 관련 프로필과 결정 내역을 전용 RPC로 함께 반영한다. 기존 승인 대상·직급·권한과 지급정책 유지.
- 고객 약속·직원 수정·권한 변경·가입 승인·설치일·스팟 검토·평가취소는 0건 변경을 성공으로 표시하지 않는다.
- 새 모바일 스팟만 source_sale_id FK로 연결해 판매 삭제 시 함께 삭제. 기존 스팟과 홈 스팟은 연결 대상을 추정하지 않는다.
- 실적입력·고객관리·홈 설치관리 및 공용 계산 분리. 실적입력·고객관리는 지연 로딩. 메인 JS 952.76 → 795.74kB(약 16.5% 감소), gzip 273.17 → 231.98kB. 전체 App 분해 완료는 아니다.
- SQL 원본: sql/atomic_sale_save_and_history.sql + sql/preserve_home_closed_history.sql + sql/atomic_admin_decisions.sql. CLI 미설치로 timestamp 파일을 임의 생성하지 않고 Supabase migration으로 기록.
- **운영 DB 적용 완료**: atomic_remaining_saves_and_preserve_closed_history / 20260916163959. staging과 운영 함수 4개 정의 해시·ACL·SECURITY INVOKER 일치. 보안 advisor는 기존 4항목 그대로이며 새 항목 없음.
- **최종 코드 검증 통과**: 단위·계약 192개 + 모킹 UI 12개 + 빌드(run 35122940327). 실제 staging Auth/Chromium/DB 6개(run 35122940435). 모바일 등록 실패 전체 롤백·재시도·수정·삭제·실행취소, 홈 반복 수정 및 이력 보존, 다른 직원 차단 검증.
- tests/db/sale-save.sql 및 admin-decisions.sql: 기존 RLS, 구버전 수정, 오래된 자료 거부, 승인 두 번째 쓰기 실패 롤백 통과. 모든 가상 fixture는 staging에서만 실행하고 롤백했다.
- E2E 종료 후 staging 직원 2명 유지, 고객·판매·주문·약속·비용·일일실적 0건 확인. 운영 직원 로그인/실데이터 fixture 테스트는 하지 않았다.
- 검증 중 발견한 React 지연로딩 선언 순서 오류와 테스트 문구 불일치는 수정 후 재검사 통과. 최종 병합 SHA, main CI 및 Vercel Production 배포 결과는 **PR #18 본문**에 기록한다.

## 확인된 운영 상태 (2026-09-16)

- PR #16은 main 1bfc95036c66907f6db40dbb3bff50defab9b1df로 병합, Vercel Production 성공 및 main CI 35111365951(190개+모킹 UI 9개+빌드) 성공 확인.
- 문서 추가 후 동일 코드 재실행 35111221677에서 첫 홈 저장은 성공했으나 새로고침 중 사라진 배지 팝업 클릭 대기로 timeout, fixture 후처리가 미완료됐다. [PR #17](https://github.com/leeblack4878-cyber/miso-incentive/pull/17)에서 팝업 대기/독립 후처리 timeout을 보완했다. 검증된 staging 가상 계정 2개의 잔여 데이터만 정리했고 운영 정책/화면 변경은 없다. 최종 회귀 및 병합 상태는 PR #17 본문과 CI에 기록한다.
- 최신 개선 PR: [#16](https://github.com/leeblack4878-cyber/miso-incentive/pull/16). 코드 SHA 8b74e924e9344cbea8528808dc45275677adc5d6에서 단위·계약 **190개**, 모킹 UI **9개**, 빌드 및 실제 Auth/Chromium/DB 홈 E2E **4개** 통과. 정책/UI run 35110245188, 실제 Auth run 35110245127.
- 운영 DB migration bind_permission_scopes_to_ids **20260916144810 적용 완료**. 전체 접근 결과 동등성 검사 통과, 함수 ACL/보안 속성 유지 재확인. 보안 advisor 항목/건수는 적용 전과 같으며 기존 경고 해소 작업은 포함하지 않았다.
- 운영은 GitHub main → Vercel 자동 배포. 최종 병합 SHA와 Production 배포 증거는 PR #16 본문에 기록한다. 이전 PR #14·#15의 기준 SHA 6e25caf47fa2f313264b38e6146fcd7ea4636393도 Production 성공 확인.
- PR #15의 실제 Auth/Chromium/DB 홈 E2E **4/4 통과**: run 35105382513. 같은 코드 트리의 main CI 35105394276: 단위·계약 186개, 모킹 UI 7개, 빌드 통과.
- 기존 “테스트 서버 키 미등록 / Auth E2E 미실행 / 배포 미확인”은 해결된 과거 상태다. 최초 E2E가 약속 소실을 검출했고 묶음 전체 판매 ID의 약속·비용을 조회하도록 고친 뒤 통과했다.
- 실제 로그인 검증은 전용 staging이며 운영 직원 계정으로 로그인한 검증과 구분한다.

## 이번 변경: ID 권한 판정과 관리자 화면 분리

- 브랜치 fix/identity-scope-and-modules. 기존 담당자 6인의 승인·활성 계정 ID를 운영에서 확인했다.
- 전사, 임원 제외 전사, 지정 상권, 권한 관리자, 지원 판매의 이름 비교를 ID 비교로 전환. 이름 변경에도 본인 범위를 유지하고 동명이인은 지정 권한을 얻지 못한다.
- 역할/활성/승인/매장 조건, 함수 실행 권한 및 SECURITY DEFINER 속성 유지. 일반 직원의 본인 데이터 제한과 지원 판매 직급 조건도 유지. 인센티브 정책·목표·금액 변경 없음.
- sql/bind_permission_scopes_to_ids.sql: 전체 viewer×target 결과 및 지원 판매/권한 관리자 결과가 적용 전후 같아야 커밋. 운영 55계정 기준 3,025쌍. 동시 프로필 변경을 잠시 막고 차이가 있으면 전체 롤백.
- CLI가 설치되지 않았고 오프라인 설치도 불가능하여 timestamp 파일을 임의 생성하지 않았다. 위 SQL을 Supabase apply_migration으로 기록한다.
- staging 적용 및 tests/db/permission-scopes.sql 통과: 전 범위, 이름 변경, 임원 제외, 허용된 세 역할×승인상태×활성 여부, 동명이인, 지원판매/다른 seller 차단. 모든 fixture 롤백.
- 기존 tests/db/sale-integrity.sql도 ID fixture로 갱신 후 통과: 삭제/승인 롤백, 반복요청, 개인 격리, 월 마감. staging에 E2E 직원 2명만 남고 판매·주문 0건 재확인.
- 지급기준·권한 관리 화면과 공통 표시 컴포넌트/상수를 App에서 분리. 관리자 탭 진입 시 지연 로딩. 추출한 함수/상수 본문이 원본과 동일함을 확인.
- 단위·계약 **190개**, 빌드 통과. 최종 메인 JS 970.19 → 952.76kB, gzip 277.09 → 273.17kB. 전체 App 분해 완료는 아니며 번들 크기 경고는 남는다.
- 이번 코드의 GitHub UI/실제 Auth CI와 운영 DB 적용은 완료. 프런트 배포는 PR #16의 최종 상태와 Production status를 확인한다. 모킹 UI 통과만으로 실제 Auth 통과를 대신하지 않는다.
- 첫 PR CI에서 ROLE_LABELS 공유 상수 누락을 직원 캘린더 브라우저 검사가 검출했다. 공용 모듈에서 App과 권한 화면 모두 가져오도록 수정했고 운영에는 실패한 버전을 반영하지 않았다.

## 유지해야 할 운영 기준

- 2026-10부터 정책 반영 전 월별 기본 입력 잠금. “○월 정책 입력 전” 표시, policy_ready_months로 해당 월만 해제. policy_blocked_months로 재잠금. 설정 조회 전/실패도 잠금.
- 이 정책 준비 잠금은 앱 입력 제어다. DB의 기존 월 마감 잠금과 별개이며 직접 API까지 막는 서버 잠금이라고 설명하지 않는다.
- 홈은 설치 완료월 실적 인정. 원자적 등록·수정·취소·삭제 RPC, RLS, 개인/매장 범위 유지.
- 홈 편집 회귀는 설치대기/완료 묶음 스마트홈 추가, 반복저장, DB 실패 롤백, 다른 직원 격리. 단위/소스계약/모킹 UI를 실제 Auth E2E라고 표현하지 않는다.
- 테스트 환경: ahmvrflklvmdprolauuy, 운영: ufkxbrzcmncwynvygaak. 운영 데이터로 fixture 테스트 금지. home-e2e 환경의 서버 키는 runner에서만 사용하고 출력하지 않는다.
- 같은 저장소의 소유자 PR에서 홈 E2E 자동 실행, 외부 포크 제외. 수동 실행도 지원한다.

## 후속 작업 범위와 유지사항

- 이전 후보인 모바일/기타 저장 원자화, 직원 정보·커리어 승인 원자화, 완료·취소 약속 보존, 실적입력·고객관리 화면 분리는 PR #18에서 처리했다.
- 주요 변경 경로의 0건 응답과 실패 안내를 보완했다. 앱 전체의 모든 저장 경로에 대해 무결성을 보증한다는 의미는 아니다.
- App에는 월별 현황·평가 등 코드가 여전히 남으며 메인 번들 크기 경고도 남는다. 이번 화면 분리로 모든 성능 개선이 끝났다고 해석하지 않는다.
- 새달 정책은 사용자 전달 후 버전·숫자 검증을 마치고 해당 월 입력을 연다. 임의 이월·단가 변경 금지.
- 지정 관리자 ID 권한 판정과 월별 정책 데이터의 인명 매칭은 별개다. 지급대상·외부 확인실적의 인명 매칭을 임의로 바꾸지 않는다.

## 다음 작업 시작

AGENTS.md → PROJECT_CONTEXT.md → HANDOFF.md → CHANGELOG.md와 최신 커밋을 읽고 실제 코드·DB·CI·배포를 대조한다. 기존 정책과 관리 범위는 유지한다.
