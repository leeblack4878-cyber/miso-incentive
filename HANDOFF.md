# MISO Incentive 인수인계

> 작업이 끝날 때마다 갱신한다. 완료된 오래된 항목은 `CHANGELOG.md`로 옮기고 이 문서는 현재 상태만 간결하게 유지한다.

## 현재 운영 상태

- 운영 브랜치: `main`
- 운영 배포: Vercel 자동 배포
- 최근 운영 반영: PR #12 병합(6f308e1), 9월 15일 특판 및 홈 브라우저 회귀 코드 배포 성공
- 최신 확인 결과: 자동 테스트 170개 통과, Vite 운영 빌드 성공
- 홈 주문 삭제 권한은 원자적 수정에 필요한 본인 행으로만 보완했으며, 다른 직원·매장 접근 범위와 지급 정책은 변경하지 않음

## 월별 정책 입력 잠금 추가 (2026-09-16, PR #14)

- 사용자 확정: 매월 말일/1일에 정책 전달. 2026-10부터 월별 정책 반영 전 기본 잠금, 캘린더에 “○월 정책 입력 전” 표시.
- 기존 `policy_blocked_months`는 수동 재잠금으로 유지, `app_config.policy_ready_months`는 해당 월 입력 열기 확인 목록. 키가 없으면 10월 이후는 기본 잠금.
- 기존 관리자 권한·정책 금액 유지. 관리자 ‘정책 반영 완료 · 입력 열기’로 해당 월만 허용. 두 설정은 한 번의 upsert로 저장하고 반환 행을 확인.
- 설정 조회 전/실패는 잠금 유지. 앱 복귀/포커스 시 상태 갱신. 모바일·홈·기타 실적 버튼 및 일일/월간 저장·대기 자동저장 차단. 기존 달력 조회와 고객 약속 기능은 유지.
- 이번 추가는 앱 입력 제어이며 DB RLS/기존 RPC 권한을 변경하지 않음. 새 정책 배포 시 코드·정책 검증 후 해당 월 입력을 열 것.
- 단위/계약 182개 통과, 빌드 통과. 실제 App 캘린더를 렌더링하는 모킹 브라우저 검사 3개 추가(UI 전체 7개); CI 결과 확인 중.

## 이번 개선 브랜치 (2026-09-16, 운영 미반영)

- PR #14: https://github.com/leeblack4878-cyber/miso-incentive/pull/14
- 브랜치 `fix/launch-readiness-integrity`, main `073c175`와 같은 트리에서 시작.
- 모바일·기타 판매 삭제: 일일 실적과 FK 연결 약속·비용·지원매장 실적을 단일 invoker RPC에서 처리. 일일 원본/판매 메타가 달라지면 중단해 재조회 안내.
- 특판 예외 승인: 판매 행 잠금, pending 상태 확인, 지급액+승인 상태 동시 처리. 반복 요청은 applied=false로 추가 지급·중복 알림 없음.
- 영업비용 삭제: 오류·반환 0건을 성공으로 표시하지 않음. 일일 저장도 실제 반환 행 확인 후 상위 합계 갱신.
- 권한 오류를 월 마감으로 단정하지 않고 HOME/Sale 오류를 직원 안내 문구로 변환.
- 오늘 업무를 홈 최상단에 배치, 조회 실패는 0건 대신 확인 실패 표시, 한국 날짜 기준 사용.
- 현재 지급기준 버전/이월 적용 표시. 정책 선택·금액·권한·RLS 변경 없음.
- 오늘 업무·비용·특판 승인 컴포넌트 및 피드백/판매 저장 로직 분리. 특판 관리 지연 로딩. App 전체 분리는 아직 완료 아님.
- 테스트 DB에 새 RPC 적용 완료. 실제 SQL/RLS에서 삭제 중간 실패 롤백, 연결 행 삭제, 원본 변경 차단, 반복 삭제, 승인 중간 실패 롤백, 승인 반복/반대 결정 재호출 중복지급 방지, 기본금액 2만원/요청금액 5만원, 타인 접근·직원 승인·월 마감 차단 통과. 테스트 데이터는 모두 롤백.
- 단위/계약 178개 및 빌드 통과. 브라우저 UI 테스트는 API 모킹 4개이며 GitHub CI에도 추가. 실제 Auth 홈 E2E와 구분할 것.
- PR #14 코드 커밋 `2e7ee53`의 GitHub Actions `35100912006`에서 단위/계약 178개·빌드 및 Chromium UI 4개 전부 통과. 320/390px 가로 넘침 없음, 오류/0건 삭제 안내 확인. Vercel PR 미리보기 성공(운영 배포 아님). 로컬 Chromium 다운로드는 네트워크 타임아웃으로 실행하지 못했으나 CI에서 실제 실행 완료.
- 신규 RPC는 SECURITY INVOKER, authenticated 전용. 기존 RLS/관리 대상 매핑은 그대로 유지. staging 보안 점검에는 기존 공개 매장 조회/권한 보조 함수 등의 경고가 남아 있으나 신규 함수 경고 없음.
- 운영 적용 순서: 신규 DB 마이그레이션 → PR 병합/프런트 배포 → 실제 계정 검증. 운영 DB/배포는 이번 작업에서 아직 변경하지 않음.

## 진행 중 / 배포 대기

- 9월 15일 정책 변경: PR #12 운영 배포 완료
- 홈 묶음 회귀: 서울 테스트 프로젝트 `ahmvrflklvmdprolauuy` 구성 완료. 실제 SQL/RLS 회귀 통과, 브라우저/Auth E2E는 서버 키 등록 전이라 미실행
- 실행 안내: `tests/e2e/README.md`, 준비/검증 기록: `tests/e2e/staging-setup.md`. GitHub `home-e2e` 환경에 테스트용 `E2E_SUPABASE_SERVICE_KEY` 하나를 등록하면 수동 CI에서 가상 직원 2명 자동 준비 후 실행
- 170개 단위/계약 테스트와 빌드는 통과. 이를 E2E 통과로 해석하지 말 것

## 최근 완료

- 9월 15일 특가·지인 정책을 별도 버전으로 추가하고 F971-256 MNP·기기변경 추가 인센티브를 각각 5만원으로 변경
- 9월 11~14일 F971-256 추가 0원 기록은 보존하고 판매일 기준으로 구·신 정책 선택을 분리
- 일반 직원의 홈 묶음 수정 시 기존 본인 주문을 RLS가 0건 처리해 `HOME_REPLACE_ORDERS_MISMATCH`가 발생하던 문제 수정
- 운영 롤백 검증에서 본인 주문 삭제 1건, 다른 직원 주문 삭제 0건을 확인하고 실제 데이터는 변경하지 않음

- 9월 11~14일 SIM MNP 활성화 정책을 개인 누적 그레이드별 건당 추가 지급으로 구현하고, 직원 입력값은 요금제 칸 구분 없이 합산하며 기간 밖 실적만 제외
- 9월 11~14일 홈 활성화 정책은 가정망·소호망을 그레이드에 합산하되 가정망만 지급하고, 가정망 1G 건당 5만원을 추가하도록 구현. 기존 4~7일 TV프리 정책은 유지
- 9월 11일 특가·지인 공지표를 기존 정책 틀에 반영하고 누락됐던 F971-256 MNP·기기변경(추가 지급 0원)을 선택지에 추가
- 점장 AA임팩트 MNP 합산 코드와 일일 브리핑 회귀 테스트의 잘못된 줄바꿈·이스케이프 복구
- 일일 브리핑의 홈 미완료 경고는 당월 청약건만 포함하고, 오늘 고객 약속·오늘 홈 설치를 실행 요청에서 우선 안내

- 9월 점장 AA 최신 현황표의 매장별 확인 실적·외부 평가값을 반영하고 MNP는 일반 MNP+SIM MNP 합산 정책을 유지
- 9월 점장 AA임팩트 회사 목표와 매장별 HS 비중 배분을 확정 영업팀 목표표 기준으로 교정

- 취소된 구버전 홈 집계를 내역·KPI·예상급여에서 제외
- 홈 본판매가 없는데 동시판매 수수료 30만 원이 남는 문제 수정
- 완료된 신규 홈 주문이 개인 화면과 관리자 직원 현황에서 0건으로 표시되던 문제 수정
- 개인 홈, 관리자 현황, 매장 목표, 월간 순위, 평가, 담당 급여가 같은 완료 홈 원본 건수를 사용하도록 통일
- 모바일 긴 문구와 작은 글씨의 카드 넘침 방지
- 일일 브리핑의 예상마감 지표를 한 줄씩 분리하고 건수형 수치를 정수로 표시
- 마지막 응원 문구를 날짜·매장과 미입력·설치 지연·부족·좋은 흐름 상황에 맞게 변경
- Supabase 로그인 재인증 시각 대신 실제 앱 실행·복귀 시각을 최종 접속으로 기록
- 직원 관리에서 관리 범위 직원의 월 개인 목표를 확인하고 즉시 새로고침 가능
- 홈 등록·수정·완료·취소·되돌리기·삭제를 원자적 RPC로 묶고 변경 건수를 검증
- 일일 브리핑이 매장 도전 목표를 우선 사용하고 없는 항목만 회사 기준으로 보완하도록 수정

## 확인이 필요한 기술 부채

- 일부 Supabase 변경 요청은 실제 변경 행 수를 재확인하지 않아 RLS 0건 처리와 성공을 구분하기 어려움
- 신규 회귀 테스트 중 일부는 브라우저·DB 통합 테스트가 아니라 소스 계약 검사임
- `src/App.jsx`와 메인 JavaScript 번들이 커서 기능 분리와 초기 로딩 개선이 필요함
- 이름 기반으로 판단하는 일부 권한 범위는 장기적으로 불변 ID 기반 검토가 필요하나, 사용자 승인 없이 변경하지 않음

## 다음 권장 순서

1. 개선 PR의 UI CI 확인 후 DB 마이그레이션과 프런트 배포를 함께 진행; 나머지 저장 경로의 원자성은 별도 단계로 검토
2. 테스트 서버 키를 GitHub 환경에 등록하고 Authenticated home bundle regression 실행; 실제 브라우저 결과를 확인·수정하고 기록
3. `App.jsx`를 작은 단위로 분리하되 단계별 테스트 후 반영
4. 오래된 `README.md`를 현재 설치·운영 방식에 맞게 갱신

## 다음 세션 시작 문구

> 작업 시작 전에 AGENTS.md, PROJECT_CONTEXT.md, HANDOFF.md, CHANGELOG.md와 최근 Git 커밋을 먼저 확인해. 현재 상태와 미완료 사항을 10줄 이내로 요약하고 이번 작업 범위를 말한 뒤 진행해. 기존 정책이나 권한은 임의로 변경하지 마.

## 종료 체크리스트

- [ ] 관련 자동 테스트 통과
- [ ] 운영 빌드 통과
- [ ] 개인·관리자·내역·급여 숫자 교차 확인
- [ ] 권한·정책값의 의도치 않은 변경 없음
- [ ] `HANDOFF.md` 갱신
- [ ] 사용자 노출 변경이면 `CHANGELOG.md` 갱신
- [ ] 배포 요청 시 Vercel 성공 확인
